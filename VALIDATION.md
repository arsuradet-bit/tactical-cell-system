# ผลตรวจชุดส่งมอบ

ตรวจเมื่อ 14 กันยายน 2026 โดยใช้ข้อมูลสังเคราะห์และตัวอย่างโครงสร้างจากผู้ใช้

## ผ่านแล้ว

- Python compile และ unit tests 11 กรณี: ตัวอย่าง G-Mon, fingerprint ซ้ำ, รหัสและ PLMN, พิกัดผิดรูปแบบ, วันเวลา ค.ศ./พ.ศ., CSV comma/semicolon/tab, CDR headers และการไม่เก็บเลขระบุตัวบุคคลในผล, G-Mon หลัก/CDR สำรอง, หลาย PLMN, รูปพัด และ escape popup
- Streamlit AppTest: เปิดหน้าได้ไม่มี exception, ค้นหา CELL/LAC/NBID แยกกัน, เงื่อนไขร่วม AND, ล้าง CDR และปิดทางเข้าสู่หน้าใช้งานเมื่อไม่มี Secrets
- ทดสอบ SQL จริงบน PostgreSQL แบบฝัง PGlite 0.3.16 ในหน่วยความจำ ผ่านสะพานคำสั่ง SQLAlchemy: schema, batch import, duplicate, latest by survey timestamp, exact search, PLMN separation, exact-pair lookup, bounds, result limit และ transaction rollback
- ทดสอบเบราว์เซอร์บน localhost: อัปโหลด CDR สังเคราะห์ 3 เหตุการณ์ ได้ G-Mon 2 จุดและ CDR สำรอง 1 จุด แสดงปุ่มเล่นเหตุการณ์และเล่นถึงเหตุการณ์ที่ 3
- กดล้าง CDR แล้วไฟล์หายจาก uploader ผลแผนที่และตัวนับกลับเป็น 0
- ตรวจหน้าเดสก์ท็อปและจอแคบด้วยข้อมูลสังเคราะห์

## ติดตั้งและทดสอบระบบจริงแล้ว

- อัปเดต GitHub main และ Streamlit เดิม ใช้ Python 3.12 พร้อม Secrets และธีมใหม่
- สำรองโค้ดเดิมไว้ที่ branch `backup-before-intel-redesign-20260914`
- เชื่อม Cloud SQL intel-db ด้วย psycopg2/SQLAlchemy และ TLS สำเร็จ สร้างตารางใหม่โดยไม่เปลี่ยนตารางต้นฉบับ
- ตรวจข้อมูลเดิม 42,914 แถว: เพิ่มประวัติไม่ซ้ำ 19,469 แถว, ข้ามซ้ำ 23,343 แถว, ไม่ผ่านการตรวจ 102 แถว ซึ่งยังอยู่ในต้นฉบับ
- ตรวจผ่าน Cloud SQL Studio: ต้นฉบับ 42,914 แถว, ประวัติใหม่ 19,469 แถว, ตัวชี้ล่าสุด 768 ชุด PLMN/XCI/LAC
- ค้นหา CELL+LAC ได้ 1 จุด และ LAC อย่างเดียวได้ 9 จุด; เวลาค้นฐานข้อมูลที่หน้าเว็บแสดงประมาณ 0.7 วินาทีในตัวอย่างนี้ ไม่รวมเวลาทั้งหมดจนแผนที่โหลดเสร็จ
- ทดสอบ CDR สังเคราะห์บนเว็บจริง 3 เหตุการณ์: จับคู่ G-Mon 2 จุด และใช้พิกัดสำรอง CDR 1 จุด
- เล่นไทม์ไลน์ถึงเหตุการณ์ 3/3 และกดล้าง CDR: ไฟล์ออกจาก uploader และผล/ตัวนับกลับเป็น 0
- ปิดโหมดย้ายข้อมูลหลังงานเสร็จแล้ว

## ยังไม่ทดสอบ

- ยังไม่ได้ทดสอบผู้ใช้พร้อมกัน 10–15 คน หรือโหลดต่อเนื่องระยะยาว
- ไม่ได้ตรวจ CSV/CDR ต้นฉบับจริง มีเพียงหัวคอลัมน์และตัวอย่างแถวที่ส่งในแชท
- แถวเดิมที่ไม่ผ่านการตรวจยังต้องตรวจแก้จากต้นฉบับก่อนนำเข้าใหม่
- ยังไม่รับรองเวลาค้นหาสำหรับทุกขนาดข้อมูลและทุกสภาพเครือข่าย

## ก่อนใช้งานจริง

ผู้ดูแลควรเปลี่ยนรหัสผ่านเดิมที่เคยอยู่ในซอร์สสาธารณะและทดสอบไฟล์ต้นฉบับจริงก่อนใช้งานเต็มองค์กร ต้องไม่เปิด INTEL_DEMO บนระบบจริง
